# data/untrusted

Content that an agent might fetch from the outside world (web pages, READMEs, tickets).
`setup_guide.md` contains a **hidden prompt injection** in an HTML comment. Notebook 07 uses it
to show why tool *results* must be screened, not just user input. Nothing here is ever executed.
